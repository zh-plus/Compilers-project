name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: clean
      working-directory: spl_project2
      run: make clean
    - name: make
      working-directory: spl_project2
      run: make
    - name: make test1
      working-directory: spl_project2
      run: make test1
